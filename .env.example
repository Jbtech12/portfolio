# Copy this file to .env and fill in your values.
# OPENAI_API_KEY is used only by the /api/chat serverless function (server-side),
# so it is never exposed in the browser bundle. Do NOT prefix it with VITE_.

OPENAI_API_KEY=your_openai_api_key_here
