# Copy this file to .env and fill in your values.
# IMPORTANT: VITE_ prefixed variables are exposed in the browser bundle.
# Anyone can read your API key by inspecting the built JavaScript.
# For production, move this call to a serverless function or backend proxy.

VITE_OPENAI_API_KEY=your_openai_api_key_here
